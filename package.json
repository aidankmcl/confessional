{
  "name": "confessional",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^1.8.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "electron-winstaller"
    ]
  }
}
